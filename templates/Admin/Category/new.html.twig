{% extends 'Admin/admin_layout.html.twig' %}

{% block content %}
<h1>Ajouter une Catégorie</h1>
{{ form_start(form) }}
    {{ form_row(form.nom) }}    {{ form_row(form.description) }}
    <button class="btn btn-ajout">Créer</button>
{{ form_end(form) }}
<a href="{{ path('admin_categories') }}" class="btn btn-delete">Retour</a>
{% endblock %}
