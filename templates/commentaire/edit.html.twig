{% extends 'base.html.twig' %}

{% block body %}
    <h2>Modifier votre commentaire</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('app_post_all') }}" class="btn btn-secondary">Annuler</a>
{% endblock %}