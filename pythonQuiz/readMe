sudo apt install python3-pip
sudo apt update
sudo apt install python3.12-venv
python3 -m venv venv
source venv/bin/activate
pip install torch


pip install fastapi uvicorn transformers
cd pythonQuiz
uvicorn app_BD:app --host 0.0.0.0 --port 8001
uvicorn TextToSpeach:app --host 0.0.0.0 --port 8002
uvicorn app_v1:app --host 0.0.0.0 --port 8003
